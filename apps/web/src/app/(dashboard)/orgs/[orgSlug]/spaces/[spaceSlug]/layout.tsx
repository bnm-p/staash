import { SpaceHeader } from "@/components/space-header";

interface ISpaceLayoutProps {
	children: React.ReactNode;
}

export default async function SpaceLayout({ children }: ISpaceLayoutProps) {
	return children;
}
